import { fromJS } from 'immutable'
import { handleNewPollLogsSaga } from '../sagas'
import * as actions from '../actions'

describe('listings sagas', async () => {
  test.skip('function: handleNewPollLogsSaga', async () => {
    console.log('stub')
  })
})
